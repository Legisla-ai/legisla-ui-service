import { ChatArea } from '@/components/ChatArea/ChatArea';

export default function RiskAnalysis() {
  return (
    <div className="h-full">
      <ChatArea mode="riskAnalysis" isSidebarOpen={false} />
    </div>
  );
}
