const theme = {
  token: {
    colorPrimary: 'var(--primary)',
    colorBgFloatButton: 'var(--accent)', // Cor de fundo do FloatButton
    colorTextFloatButton: 'var(--accent-foreground)', // Cor do ícone/texto
  },
};

export default theme;
